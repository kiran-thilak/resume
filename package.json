{
  "name": "resume",
  "version": "1.0.0",
  "description": "Markdown-driven resume site with an ATS-safe PDF build",
  "main": "src/server.js",
  "scripts": {
    "dev": "npm run css:build && concurrently -k -n server,css \"node src/server.js\" \"npm run css:watch\"",
    "start": "node src/server.js",
    "css:build": "vite build",
    "css:watch": "vite build --watch",
    "pdf": "node src/pdf/generate.js",
    "pdf:check": "node scripts/check-pdf.js",
    "build": "npm run css:build",
    "build:pdf": "npm run css:build && npm run pdf && npm run pdf:check"
  },
  "dependencies": {
    "marked": "^13.0.0"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "autoprefixer": "^10.5.4",
    "concurrently": "^10.0.4",
    "tailwindcss": "^3.4.19",
    "vite": "^8.2.0"
  }
}
